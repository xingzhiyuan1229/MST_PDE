# 论文《Multi-scale time-stepping of Partial Differential Equations with transformers》公式解读

> 对象文件：`论文.md`  
> 目标：对文中全部公式与数学符号逐一解释，补充必要推导，并说明公式之间的内在联系。  
> 说明：按论文章节顺序组织；所有公式和符号统一用 `$$...$$` 渲染。

---

## Abstract（涉及核心数学对象的先导说明）

摘要没有给出编号公式，但已经隐含了论文的数学主线：

1. 用神经网络学习偏微分方程时间演化；
2. 先做空间编码再做时间推进；
3. 通过多时间尺度降低累计误差。

这三点在后文分别落在：

- PDE通式与算子学习：$$u_t = \mathcal{N}(\cdot)$$；
- 编码-推进-解码：$$z_t=P(s_t),\ z_{t+\Delta t}=D^{\Delta t}(z_t),\ \bar s_t=Q(z_t)$$；
- 多尺度：$$\Delta t\in\{1,2,4,8\}$$。

---

## 1. Introduction

### 1.1 PDE通式（Eq. (1)）

原式：

$$
\mathbf{x}=[x_1,x_2,\ldots,x_d]^T\in\mathbb{R}^d,\quad t\in\mathbb{R}
$$

$$
u_t = N\big(x,u,u_{x_1},u_{x_2},\ldots,u_{x_1x_1},u_{x_1x_2},u_{x_2x_2},\ldots\big) \tag{1}
$$

> 说明：文中使用的是 $$u_t$$，这里以同义写法呈现时应理解为同一对象（状态 $$u$$ 对时间的一阶导）。

#### 解释

- $$u(\mathbf{x},t)$$：待求解的场（如涡量、速度分量、温度等）。
- $$u_t$$：时间变化率，表示系统“下一瞬间”如何由当前状态决定。
- $$N(\cdot)$$：右端非线性算子，统一囊括对流项、扩散项、源项以及更高阶空间导数耦合。

#### 必要推导（从连续动力系统到离散时间推进）

对一般演化方程

$$
\frac{\partial u}{\partial t}=N(u)
$$

在时间上做一步显式离散（Euler思想）可得：

$$
u(t+\Delta t)\approx u(t)+\Delta t\,N\big(u(t)\big)
$$

这一步是后续所有“学习时间推进算子”的理论起点：网络本质在近似映射

$$
\mathcal{T}_{\Delta t}:u_t\mapsto u_{t+\Delta t}.
$$

#### 与后文关系

- Eq. (1) 给出物理系统的“真演化规则”；
- 后文ROM、latent模型、attention模型都在逼近这个规则的不同表示。

---

### 1.2 ROM分解（Eq. (2)）

原式：

$$
u(x,t)=a(t)\cdot\psi(x)=\sum_{k=1}^{K}a_k(t)\psi_k(x) \tag{2}
$$

并且文中给出空间基所在空间：

$$
\psi\in\mathbb{R}^{K}
$$

#### 解释

- $$\psi_k(x)$$：空间基（主模态）；
- $$a_k(t)$$：对应模态的时间系数；
- $$K$$：保留模态数。

其含义是：高维场被投影到低维基底，空间复杂度由 $$\psi_k$$ 承担，时间复杂度由 $$a_k$$ 承担。

#### 必要推导（投影思想）

设近似解写作

$$
u(x,t)\approx\sum_{k=1}^{K}a_k(t)\psi_k(x).
$$

把它代回控制方程并与测试函数（常取同一组基）做内积，得到关于 $$a_k(t)$$ 的低维动力系统：

$$
\left\langle \sum_{k=1}^{K}\dot a_k(t)\psi_k,\psi_i\right\rangle
=
\left\langle N\left(\sum_{k=1}^{K}a_k(t)\psi_k\right),\psi_i\right\rangle,
\quad i=1,\ldots,K.
$$

这就是“无限维PDE”到“有限维ODE系统”的标准降阶路径。

#### 与后文关系

- latent space time-stepping 是ROM思想的神经网络泛化：基底由网络学习，不再限于线性子空间。

---

### 1.3 Latent space time-stepping（行内公式）

原式集合：

$$
P,\ Q,\ D^{\Delta t}
$$

$$
z_t=P(s_t)
$$

$$
Q(z_t)=\bar s_t
$$

$$
z_{t+\Delta t}=D^{\Delta t}(z_t)
$$

$$
s_t,\ \hat s_t,\ z_t
$$

#### 解释

- $$P$$：编码器，把原状态 $$s_t$$ 映射到紧致表征 $$z_t$$；
- $$D^{\Delta t}$$：在潜空间中做时间推进；
- $$Q$$：解码器把潜变量还原到物理空间。

#### 必要推导（算子分解）

原目标是直接逼近

$$
\mathcal{T}_{\Delta t}: s_t\mapsto s_{t+\Delta t}.
$$

论文采用的分解可写为

$$
\mathcal{T}_{\Delta t}\approx Q\circ D^{\Delta t}\circ P.
$$

该分解减少了时间推进模块处理的维度，且把“表示学习”和“动力学学习”分离，利于训练稳定性和效率。

#### 与后文关系

- 2.1 中 CAE 对应 $$P,Q$$；
- 2.2/2.3 中 attention+transformer 对应 $$D^{\Delta t}$$；
- 2.4 中损失函数直接约束 $$\bar z$$ 或重建误差。

---

### 1.4 Direct time-stepping（行内公式）

关键符号：

$$
P=I,\quad Q=I
$$

#### 解释

当编码器、解码器取恒等映射时，问题退化为在原网格空间直接学习：

$$
s_{t+\Delta t}=\tilde D^{\Delta t}(s_t).
$$

这通常精度高，但计算成本更高（状态维度大）。

#### 与后文关系

本文强调先压缩到潜空间再推进，正是为降低attention复杂度。

---

### 1.5 Solution function approximation（PINN行内公式）

原式：

$$
u(x,t)\approx f_{\phi}(x,t)
$$

#### 解释

- 这是“函数拟合视角”：直接拟合连续解函数，而非学习离散步进算子。
- 参数 $$\phi$$ 由数据损失+物理残差损失共同训练。

#### 必要推导（残差构造思想）

若原方程写作

$$
\mathcal{R}[u]:=u_t-N(\cdot)=0,
$$

则PINN常用目标形如

$$
\mathcal{L}(\phi)=\lambda_d\mathcal{L}_{data}(f_\phi)+\lambda_p\|\mathcal{R}[f_\phi]\|^2.
$$

本文不走这一路线，而是走“离散时间推进+潜空间动力学”。

#### 与后文关系

该公式用于方法谱系对比，帮助定位本文方法在PDE学习版图中的位置。

---

## 2. Methodology

### 2.1 Convolutional autoencoder（状态与编码空间）

原文符号：

$$
s\in\mathbb{R}^{N_x\times N_y}
$$

$$
z\in\mathbb{R}^{n_x\times n_y\times d_f}
$$

#### 解释

- $$N_x\times N_y$$：原始空间分辨率；
- $$n_x\times n_y$$：降采样后的空间分辨率；
- $$d_f$$：每个空间位置的特征通道数。

#### 必要推导（维度压缩）

CAE在空间维度上执行多次下采样。设 $$m$$ 为下采样层数，则

$$
n_x=\frac{N_x}{2^m},\quad n_y=\frac{N_y}{2^m}
$$

于是token数由

$$
N_xN_y\ \to\ n_xn_y
$$

显著减小，直接降低后续attention的二次复杂度。

#### 与后文关系

2.2 注意力输入长度就是 $$n=n_xn_y$$，因此2.1是2.2计算开销可控的前提。

---

### 2.2 Attention机制总式（Eq. (3)）

预备符号：

$$
n=n_xn_y
$$

$$
f_i\in\mathbb{R}^{d_f},\quad i=0,1,2,\ldots,n-1
$$

$$
p_i\in\mathbb{R}^{d_p}
$$

$$
p=[x,y]\in\mathbb{R}^2
$$

主公式：

$$
\Delta f_i:=\sum_{j=0}^{n-1}\kappa(p_i,p_j,f_i,f_j)V(f_j),\ \forall i\in\{0,1,\ldots,n-1\} \tag{3}
$$

文中同时给出函数映射类型：

$$
\kappa:\mathbb{R}^{2(d_p+d_f)}\to\mathbb{R}
$$

$$
V:\mathbb{R}^{d_f}\to\mathbb{R}^{d_f}
$$

#### 解释

- $$f_i$$：第 $$i$$ 个位置的特征；
- $$\kappa$$：注意力核，决定第 $$j$$ 个位置对 $$i$$ 的影响强度；
- $$V$$：值映射，把输入特征投影到可聚合子空间；
- $$\Delta f_i$$：聚合后的增量更新。

这一式本质是“全局加权求和算子”。

#### 必要推导（离散积分到求和）

从连续核算子出发（见Eq. (4)），对空间积分做求积离散：

$$
\int_D g(\bar x)\,d\bar x\ \approx\ \sum_j w_j g(\bar x_j).
$$

把权重和核并入同一项，就得到Eq. (3)形式。

#### 与后文关系

- Eq. (5)/(6)/(7)分别给出 $$\kappa$$ 的不同参数化；
- Transformer多头即并行多个这样的 $$\kappa$$。

---

### 2.2 Kernel integral operator（Eq. (4)）

原式：

$$
(K_\phi(a)v)(x):=\int_D \kappa_\phi\big(x,\bar x,a(x),a(\bar x)\big)v(\bar x)\,d\bar x,\ \forall x\in D \tag{4}
$$

#### 解释

该式说明：输出点 $$x$$ 的值是对整个定义域 $$D$$ 上信息的核加权积分。它是神经算子框架的核心表达。

- $$a$$：条件函数/上下文函数；
- $$v$$：被作用函数；
- $$\kappa_\phi$$：可学习核。

#### 必要推导（与Eq. (3)的对应）

对 $$D$$ 取离散点 $$\{x_j\}_{j=0}^{n-1}$$，则

$$
(K_\phi(a)v)(x_i)\approx\sum_{j=0}^{n-1}\kappa_\phi(x_i,x_j,a_i,a_j)v_j\,w_j.
$$

将 $$w_j$$ 吸收到核或值映射中，记作

$$
\kappa(p_i,p_j,f_i,f_j)V(f_j),
$$

即Eq. (3)。

#### 与后文关系

Eq. (4)提供“连续算子观点”，Eq. (3)提供“可计算离散实现”。

---

### 2.2 Classical attention参数化（Eq. (5)）

原式：

$$
\kappa(p_i,p_j,f_i,f_j)=Q(f_i+p_i)\cdot K(f_j+p_j) \tag{5}
$$

并有：

$$
Q,\ K\ \text{为线性映射},\quad V\ \text{为线性值映射}.
$$

#### 解释

这是标准dot-product注意力思路：

- query：$$Q(f_i+p_i)$$；
- key：$$K(f_j+p_j)$$；
- 相似度：内积 $$\cdot$$。

位置与内容先相加再投影，二者耦合较强。

#### 必要推导（矩阵形式）

将所有token堆叠为矩阵后，注意力权重可写为

$$
A\propto (QX)(KX)^T,
$$

再经softmax与 $$VX$$ 相乘完成聚合。

#### 与后文关系

Eq. (6)是在此基础上的改造：把位置影响从“相加耦合”改为“乘性解耦调制”。

---

### 2.2 Proposed attention参数化（Eq. (6)）

原式：

$$
\kappa(p_i,p_j,f_i,f_j)=G(p_i,p_j)\big(Q(f_i)\cdot K(f_j)\big) \tag{6}
$$

并有：

$$
G:\mathbb{R}^{2d_p}\to\mathbb{R}
$$

#### 解释

- 先做纯内容相似度 $$Q(f_i)\cdot K(f_j)$$；
- 再用纯位置函数 $$G(p_i,p_j)$$ 做乘性调制。

这样做的直觉是“内容相似性”和“几何相对关系”分离建模，更容易解释与控制。

#### 必要推导（从Eq. (5)到Eq. (6)）

Eq. (5) 等价于把位置编码混入输入后再统一线性变换；
Eq. (6) 则相当于引入因子分解核：

$$
\kappa = \underbrace{\kappa_{pos}(p_i,p_j)}_{G}\cdot
\underbrace{\kappa_{feat}(f_i,f_j)}_{Q(f_i)\cdot K(f_j)}.
$$

这是一个更清晰的“可分离核”结构。

#### 与后文关系

Eq. (7)进一步约束 $$G$$ 的结构，使其符合相对位置不变性的物理直觉。

---

### 2.2 Relative positional encoder（Eq. (7)）

原式：

$$
G(p_i,p_j)=G\big(p_j-p_i,\ |p_i-p_j|\big) \tag{7}
$$

#### 解释

该式强调 $$G$$ 只依赖相对位移，而不依赖绝对坐标原点，体现平移等变/不变思想。

- $$p_j-p_i$$：方向信息；
- $$|p_i-p_j|$$：距离尺度信息。

#### 必要推导（对称性约束）

若系统统计性质与平移无关，则对于任意平移向量 $$c$$，应有

$$
G(p_i+c,p_j+c)=G(p_i,p_j).
$$

满足该条件的最自然参数化就是只使用相对坐标差。

#### 与后文关系

这一步让attention更符合物理场局部-相对交互的先验，提升泛化稳定性。

---

### 2.3 Transformer（行内符号）

关键符号：

$$
Q,K,V,G
$$

#### 解释

多头注意力是多个独立头并行计算：

$$
\mathrm{head}_h=\mathrm{Attn}_h(Q_h,K_h,V_h,G_h),\quad h=1,\ldots,H
$$

再拼接：

$$
\mathrm{MHA}(X)=\mathrm{Concat}(\mathrm{head}_1,\ldots,\mathrm{head}_H)W_o.
$$

每个头学习不同交互模式，提升表达能力。

#### 与后文关系

它是 $$D^{\Delta t}$$ 的核心实现模块。

---

### 2.4 Assembly and training（多尺度推进符号）

关键符号与集合：

$$
P,\ Q,\ D^{\Delta t}
$$

$$
\Delta t=\{1,2,4,8\}
$$

#### 解释

论文训练多个时间步长推进器：短步长模型更易学，长步长模型更高效；测试时可组合步长，兼顾速度与误差。

#### 必要推导（多尺度组合可达性）

目标总步长记为 $$T$$，若

$$
T=\sum_m c_m\Delta t_m,\quad c_m\in\mathbb{N},\ \Delta t_m\in\{1,2,4,8\},
$$

则可通过依次应用对应推进器得到近似终态：

$$
z_{t+T}\approx (D^{\Delta t_1})^{c_1}\circ\cdots\circ(D^{\Delta t_M})^{c_M}(z_t).
$$

这就是文中多尺度时间步进提速的数学基础。

---

### 2.4 nRMSE损失（Eq. (8)）

原式：

$$
nRMSE(\bar z,z)=\frac{\|\bar z-z\|_2}{\|z\|_2} \tag{8}
$$

#### 解释

- 分子：预测误差的二范数；
- 分母：真值规模归一化；
- 结果是无量纲相对误差，便于跨样本比较。

#### 必要推导（从RMSE到nRMSE）

标准RMSE可写为

$$
RMSE=\sqrt{\frac{1}{n}\sum_{i=1}^n(\bar z_i-z_i)^2}
=\frac{1}{\sqrt n}\|\bar z-z\|_2.
$$

若同时以真值尺度归一化（并把常数因子吸收），得到

$$
nRMSE\propto\frac{\|\bar z-z\|_2}{\|z\|_2},
$$

即Eq. (8)使用形式。

#### 与后文关系

这是训练与评估统一指标，贯穿AE与动态模型训练。

---

### 2.4 优化超参数（行内公式）

原文参数：

$$
\text{lr}=0.001
$$

$$
\text{factor}=0.2
$$

$$
\text{patience}=5
$$

#### 解释

- 初始学习率 $$0.001$$：常见Adam稳定起点；
- 平台期衰减系数 $$0.2$$：将学习率乘以 $$0.2$$；
- 忍耐轮数 $$5$$：连续若干轮不改善再衰减。

#### 与公式系统关系

它们不改动物理方程本身，但直接决定Eq. (8)最小化路径与收敛速度。

---

## 3. Experiments and discussion

### 3.1 Navier–Stokes实验方程组（涡量形式）

原方程组：

$$
\partial_t\omega(x,t)+u(x,t)\cdot\nabla\omega(x,t)=\nu\Delta\omega(x,t)+f(x),\quad x\in(0,1)^2,\ t\in(0,T]
$$

$$
\nabla\cdot u(x,t)=0,\quad x\in(0,1)^2,\ t\in(0,T]
$$

$$
\omega(x,0)=\omega_0(x),\quad x\in(0,1)^2
$$

#### 逐式解释

1. 演化方程：
   - $$\partial_t\omega$$：局部时间变化；
   - $$u\cdot\nabla\omega$$：对流输运；
   - $$\nu\Delta\omega$$：黏性扩散；
   - $$f(x)$$：外部强迫。
2. 不可压约束 $$\nabla\cdot u=0$$：保证速度场无散。
3. 初值条件 $$\omega_0$$：确定唯一演化轨道起点。

#### 必要推导（与Eq. (1)统一）

把右端合并为算子：

$$
N(\omega)= -u\cdot\nabla\omega+\nu\Delta\omega+f,
$$

则第一式就是 Eq. (1) 的具体实例：

$$
\omega_t=N(\omega).
$$

#### 与全文关系

- 这一组方程定义了数据生成的物理真值；
- 本文模型学习的是其离散时间演化算子近似。

---

### 3.2 计算效率段中的分辨率公式

原文量级对比：

$$
4\times4
$$

$$
64\times64
$$

#### 解释与推导

注意力计算通常与token数平方相关：

$$
\mathcal{O}(n^2),\quad n=n_xn_y.
$$

若原分辨率为 $$64\times64$$，则

$$
n_{raw}=4096.
$$

若编码后为 $$4\times4$$，则

$$
n_{lat}=16.
$$

二次复杂度比约为

$$
\left(\frac{4096}{16}\right)^2=256^2=65536.
$$

这解释了为何“先空间压缩再attention”能显著提升效率。

---

## 4. Conclusion and future work（公式关联总结）

本章无新公式，但可以把前述公式链条闭合为一个统一叙事：

1. 物理起点（连续动力学）：

$$
u_t=N(\cdot)\quad\text{或其NS具体形式}.$$

2. 表示分解（降维思想）：

$$
u\approx\sum_{k=1}^{K}a_k\psi_k\quad\Rightarrow\quad\text{神经泛化为} \ z=P(s).$$

3. 离散推进（算子学习）：

$$
z_{t+\Delta t}=D^{\Delta t}(z_t),\quad s\approx Q(z).$$

4. 推进器内部机制（注意力/核算子）：

$$
\text{连续核积分 }(Eq.4)\ \to\ \text{离散求和 }(Eq.3)\ \to\ \kappa\text{参数化 }(Eq.5\sim Eq.7).
$$

5. 训练目标：

$$
\min\ nRMSE(\bar z,z)=\min\frac{\|\bar z-z\|_2}{\|z\|_2}.
$$

6. 工程加速：

$$
\Delta t\in\{1,2,4,8\},\quad \text{多尺度组合推进减少计算并抑制累计误差}.
$$

这就是论文所有公式之间的“内在联系图”：

$$
\text{PDE物理规律}
\ \Rightarrow\
\text{低维表示}
\ \Rightarrow\
\text{注意力算子推进}
\ \Rightarrow\
\text{归一化误差训练}
\ \Rightarrow\
\text{多尺度高效预测}.
$$

---

## 附录：文中全部关键符号速查（按出现顺序）

$$
\mathbf{x},\ t,\ u,\ u_t,\ u_{x_i},\ u_{x_ix_j},\ N(\cdot)
$$

$$
\psi_k,\ a_k(t),\ K
$$

$$
P,\ Q,\ D^{\Delta t},\ s_t,\ \hat s_t,\ z_t
$$

$$
f_\phi(x,t)
$$

$$
N_x,N_y,n_x,n_y,d_f
$$

$$
f_i,p_i,d_p,\kappa,V,\Delta f_i,K_\phi
$$

$$
Q,K,V,G
$$

$$
\Delta t\in\{1,2,4,8\}
$$

$$
\bar z,z,nRMSE
$$

$$
\omega,u,\nu,\nabla,\Delta,f,\omega_0
$$
