# 论文《Multi-scale time-stepping of Partial Differential Equations with transformers》公式-代码对应

> 仓库：`MST_PDE`（当前远端仓库）  
> 论文文件：`论文.md`  
> 说明：按论文章节顺序整理；每条均为“代码在前、公式在后”；所有公式统一用 `$$...$$` 渲染。

---

## 1. Introduction

### 1.1 Eq. (1) PDE通式

**对应最底层代码（本仓库中与该方程最接近的“计算落点”是：模型学习数据驱动时间推进，而非显式离散求解该PDE）：**

- GitHub链接（数据读取）：
  - /blob/main/Train.py#L72-L87
- GitHub链接（时间推进学习）：
  - /blob/main/ModelClass.py#L289-L295
- 本地文件：
  - `Train.py`
  - `ModelClass.py`

```python
# Train.py (L72-L87)
benchmark = args.benchmark
if benchmark == 1:
    file_name = 'MST_NS_v1e-3_N1200_T50.npy'
elif benchmark == 2:
    file_name = 'MST_NS_v1e-4_N1200_T30.npy'
elif benchmark == 3:
    file_name = 'MST_NS_v1e-5_N1200_T20.npy'

elif benchmark == 7:
    file_name = 'KFvorticity_Re40_N200_T500.npy'

if benchmark>0:
    data_array = load_data(file_name)
else:
    data_array = np.random.randn(192,11,64,64).astype(np.float32)
```

```python
# ModelClass.py (L289-L295)
xs = [self.encoder(x.to(device)) for x in xs]
latent = xs[0]
loss = 0.
for t in range(1, rollout+1):
    latent = model(latent)
    loss = loss + criterion(latent, xs[t])
loss = loss / rollout
```

**对应公式：**

$$
\boldsymbol{x}=[x_{1}, x_{2}, ..., x_{d}]^{T} \in \mathbb{R}^{d}, \quad t \in \mathbb{R}
$$

$$
u_{t}=N(x,u,u_{x_{1}},u_{x_{2}},... ,u_{x_{1}x_{1}},u_{x_{1}x_{2}},u_{x_{2}x_{2}},... ) \tag{1}
$$

---

### 1.2 Eq. (2) ROM分解

**对应最底层代码（在本仓库中由“编码器压缩 + 动力学模型推进 + 解码器重建”实现ROM思想，而非显式展开基函数和时间系数）：**

- GitHub链接（Encoder）：
  - /blob/main/Model.py#L392-L416
- GitHub链接（Decoder）：
  - /blob/main/Model.py#L419-L444
- GitHub链接（潜空间推进）：
  - /blob/main/ModelClass.py#L390-L393

```python
# Model.py (L392-L416)
class CNN_Encoder(nn.Module):
    def __init__(self,
                 channels = [1, 8, 16, 32, 64],
                 act = nn.LeakyReLU,
                 padding_mode = 'zeros'
                 ):
        super().__init__()
        n = len(channels) - 1
        layers = []
        for i in range(n):
            layers += [
                nn.Conv2d(in_channels = channels[i],
                          out_channels = channels[i+1],
                          kernel_size = 3,
                          stride = 1,
                          padding = 'same',
                          padding_mode = padding_mode
                          ),
                nn.AvgPool2d(2),
                act()
                ]
        self.net = nn.Sequential(*layers[:-1])

    def forward(self, x):
        return self.net(x)
```

```python
# Model.py (L419-L444)
class CNN_Decoder(nn.Module):
    def __init__(self,
                 channels = [64, 32, 16, 8, 1],
                 act = nn.LeakyReLU,
                 padding_mode = 'zeros',
                 upsample_mode = 'bilinear'
                 ):
        super().__init__()
        n = len(channels) - 1
        layers = []
        for i in range(n):
            layers += [
                nn.Upsample(scale_factor=2, mode=upsample_mode, align_corners=False),
                nn.Conv2d(in_channels = channels[i],
                          out_channels = channels[i+1],
                          kernel_size = 3,
                          stride = 1,
                          padding = 'same',
                          padding_mode = padding_mode
                          ),
                act()
                ]
        self.net = nn.Sequential(*layers[:-1])

    def forward(self, x):
        return self.net(x)
```

```python
# ModelClass.py (L390-L393)
encoded = self.encoder(x.to(device).unsqueeze(0))
encoded_next = self.models[str(dt)](encoded)
decoded_next = self.decoder(encoded_next).squeeze(0)
return decoded_next
```

**对应公式：**

$$
u(x, t)=a(t) \cdot \psi (x)=\sum _{k=1}^{K}a_{k}(t)\psi _{k}(x) \tag{2}
$$

---

### 1.3 Latent space time-stepping（行内公式）

**对应最底层代码：**

- GitHub链接：
  - /blob/main/ModelClass.py#L390-L393
  - /blob/main/ModelClass.py#L289-L295

```python
# ModelClass.py (L390-L393)
encoded = self.encoder(x.to(device).unsqueeze(0))
encoded_next = self.models[str(dt)](encoded)
decoded_next = self.decoder(encoded_next).squeeze(0)
return decoded_next
```

```python
# ModelClass.py (L289-L295)
xs = [self.encoder(x.to(device)) for x in xs]
latent = xs[0]
loss = 0.
for t in range(1, rollout+1):
    latent = model(latent)
    loss = loss + criterion(latent, xs[t])
loss = loss / rollout
```

**对应公式：**

$$
z_{t}=P(s_{t})
$$

$$
Q(z_{t})=\bar{s}_{t}
$$

$$
z_{t+\Delta t}=D^{\Delta t}(z_{t})
$$

---

### 1.4 Solution function approximation（PINN行内公式）

**对应最底层代码：**

- 本仓库未实现PINN的显式残差最小化；该式在论文中用于方法综述对比，不对应仓库内具体训练算子。

**对应公式：**

$$
u(x, t) \approx f_{\phi}(x, t)
$$

---

## 2. Methodology

### 2.1 CAE表示空间（行内公式）

**对应最底层代码：**

- GitHub链接（状态张量进入网络）：
  - /blob/main/Dataset.py#L20-L34
- GitHub链接（编码后潜表示）：
  - /blob/main/Model.py#L415-L416

```python
# Dataset.py (L20-L34)
# add channel dimension (N, T, C, H, W)
self.data = self.data.unsqueeze(2)

...

def __getitem__(self, idx):
    n, t = self.hashmap(idx)
    out = [self.data[self.idxs[n], t+i*self.dt] for i in range(self.rollout+1)]
    return out
```

```python
# Model.py (L415-L416)
def forward(self, x):
    return self.net(x)
```

**对应公式：**

$$
s \in \mathbb{R}^{N_{x} \times N_{y}}
$$

$$
z \in \mathbb{R}^{n_{x} \times n_{y} \times d_{f}}
$$

---

### 2.2 Attention输入重排（行内公式）

**对应最底层代码：**

- GitHub链接（flatten为token并做注意力）：
  - /blob/main/Model.py#L361-L364
- GitHub链接（位置网格与相对坐标）：
  - /blob/main/Model.py#L151-L163

```python
# Model.py (L361-L364)
attn = torch.einsum('bhdn,bhdm->bhnm',
                    q.reshape(B, self.num_heads, self.head_dim, -1),
                    k.reshape(B, self.num_heads, self.head_dim, -1)
                    ) * self.scale
```

```python
# Model.py (L151-L163)
nx, ny = x.shape[-2:]
x_mesh, y_mesh = torch.linspace(0, 1, nx+1)[1:], torch.linspace(0, 1, ny+1)[1:]
x_mesh, y_mesh = torch.meshgrid(x_mesh, y_mesh) # (nx, ny)
x_mesh, y_mesh = x_mesh.flatten(), y_mesh.flatten()
dx = x_mesh[None, :] - x_mesh[:, None]
dy = y_mesh[None, :] - y_mesh[:, None]
r = (dx**2 + dy**2)**0.5

if self.r_only:
    return r[..., None]

theta = torch.atan2(dy, dx)
return torch.stack([r, theta, dx, dy, dx.abs(), dy.abs()], dim=-1)
```

**对应公式：**

$$
n=n_{x}n_{y}
$$

$$
f_{i} \in \mathbb{R}^{d_{f}},\quad i=0,1,2,...,n-1
$$

$$
p_{i} \in \mathbb{R}^{d_{p}}
$$

$$
p=[x,y] \in \mathbb{R}^{2}
$$

---

### 2.3 Eq. (3) 注意力通式

**对应最底层代码：**

- GitHub链接（注意力权重与值聚合）：
  - /blob/main/Model.py#L361-L386

```python
# Model.py (L361-L386)
attn = torch.einsum('bhdn,bhdm->bhnm',
                    q.reshape(B, self.num_heads, self.head_dim, -1),
                    k.reshape(B, self.num_heads, self.head_dim, -1)
                    ) * self.scale

if self.attn_method in [2,3,4,5]:
    pos_embedding = self.pos_embedder(x)

if self.attn_method == 2:
    attn = pos_embedding + attn
elif self.attn_method == 3:
    attn = pos_embedding * attn

if self.softmax:
    attn = attn.softmax(dim=-1)

if self.attn_method == 4:
    attn = pos_embedding + attn
elif self.attn_method == 5:
    attn = pos_embedding * attn

attn = self.attn_drop(attn)
x = torch.einsum('bhnm,bhdm->bhdn',
                 attn,
                 v.reshape(B, self.num_heads, self.head_dim, -1)
                 ).flatten(1, 2).reshape(B, C, H, W)
```

**对应公式：**

$$
\Delta f_{i}:=\sum_{j=0}^{n-1} \kappa(p_{i}, p_{j}, f_{i}, f_{j}) V(f_{j}), \forall i \in\{0,1, ..., n-1\} \tag{3}
$$

---

### 2.4 Eq. (4) 核积分算子对应

**对应最底层代码（离散近似）：**

- GitHub链接：
  - /blob/main/Model.py#L361-L386

```python
# Model.py (L361-L386)
attn = torch.einsum('bhdn,bhdm->bhnm',
                    q.reshape(B, self.num_heads, self.head_dim, -1),
                    k.reshape(B, self.num_heads, self.head_dim, -1)
                    ) * self.scale
...
x = torch.einsum('bhnm,bhdm->bhdn',
                 attn,
                 v.reshape(B, self.num_heads, self.head_dim, -1)
                 ).flatten(1, 2).reshape(B, C, H, W)
```

**对应公式：**

$$
(K_{\phi}(a) v)(x):=\int_{D} \kappa_{\phi}(x, \overline{x}, a(x), a(\overline{x})) v(\overline{x}) d \overline{x}, \forall x \in D \tag{4}
$$

---

### 2.5 Eq. (5) 原始注意力形式

**对应最底层代码：**

- GitHub链接（Q/K线性映射）：
  - /blob/main/Model.py#L321-L326
- GitHub链接（dot-product attention）：
  - /blob/main/Model.py#L361-L375
- GitHub链接（将位置编码加到输入，`x+pe`）：
  - /blob/main/Model.py#L357-L359

```python
# Model.py (L321-L326)
self.Q = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
self.K = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
self.V = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
```

```python
# Model.py (L357-L359, L361-L375)
if self.attn_method == 1:
    x = x + self.pos_embedder(x)
q, k, v = self.Q(x), self.K(x), self.V(x)

attn = torch.einsum('bhdn,bhdm->bhnm',
                    q.reshape(B, self.num_heads, self.head_dim, -1),
                    k.reshape(B, self.num_heads, self.head_dim, -1)
                    ) * self.scale
if self.softmax:
    attn = attn.softmax(dim=-1)
```

**对应公式：**

$$
\kappa(p_{i},p_{j},f_{i},f_{j})=Q(f_{i}+p_{i})\cdot K(f_{j}+p_{j}) \tag{5}
$$

---

### 2.6 Eq. (6) 本文采用的解耦位置-特征注意力

**对应最底层代码：**

- GitHub链接（位置编码网络$G$）：
  - /blob/main/Model.py#L122-L141
- GitHub链接（乘性融合`G * (Q·K)`）：
  - /blob/main/Model.py#L366-L373

```python
# Model.py (L122-L141)
class Functional_rel_pos_embedding(nn.Module):
    def __init__(self, num_heads=8, hidden_dim=64, r_only=True, periodic=False):
        super().__init__()
        self.periodic = periodic
        self.r_only = r_only
        self.net = nn.Sequential(
            nn.LazyLinear(hidden_dim),
            nn.LeakyReLU(),
            nn.Linear(hidden_dim, num_heads)
            )
```

```python
# Model.py (L366-L373)
if self.attn_method in [2,3,4,5]:
    pos_embedding = self.pos_embedder(x)

if self.attn_method == 2:
    attn = pos_embedding + attn
elif self.attn_method == 3:
    attn = pos_embedding * attn
```

**对应公式：**

$$
\kappa(p_{i},p_{j},f_{i},f_{j})=G(p_{i}, p_{j})(Q(f_{i})\cdot K(f_{j})) \tag{6}
$$

$$
G: \mathbb{R}^{2 d_{p}} \to \mathbb{R}
$$

---

### 2.7 Eq. (7) 相对位置编码约束

**对应最底层代码：**

- GitHub链接（使用相对位移`dx,dy`与绝对值`|dx|,|dy|`）：
  - /blob/main/Model.py#L155-L163

```python
# Model.py (L155-L163)
dx = x_mesh[None, :] - x_mesh[:, None]
dy = y_mesh[None, :] - y_mesh[:, None]
r = (dx**2 + dy**2)**0.5

if self.r_only:
    return r[..., None]

theta = torch.atan2(dy, dx)
return torch.stack([r, theta, dx, dy, dx.abs(), dy.abs()], dim=-1)
```

**对应公式：**

$$
G(p_{i},p_{j})=G(p_{j}-p_{i},\left| p_{i}-p_{j}\right| ) \tag{7}
$$

---

### 2.8 Transformer多头与层堆叠（行内公式）

**对应最底层代码：**

- GitHub链接（每层独立头数配置）：
  - /blob/main/Model.py#L457-L497
- GitHub链接（每层残差推进）：
  - /blob/main/Model.py#L510-L515

```python
# Model.py (L457-L497)
num_heads = [8, 8, 8, 8],
...
self.attn_layers = nn.ModuleList([
    Attention2D(
        dim,
        num_heads = num_heads[i],
        split = split,
        attn_method = attn_method,
        softmax = softmax,
        pos_type = pos_type,
        attn_rng = attn_rng,
        qkv_bias = qkv_bias,
        qk_scale = qk_scale,
        attn_drop = attn_drop,
        proj_drop = proj_drop
        )
    for i in range(self.n_layers)
    ])
```

```python
# Model.py (L510-L515)
for i in range(self.n_layers):
    x = x + self.attn_layers[i](self.attn_norms[i](x))
    if self.use_fc:
        x = x + self.fc_layers[i](self.fc_norms[i](x))
return x
```

**对应公式（论文描述中出现的符号）：**

$$
Q, K, V, G
$$

---

### 2.9 多时间尺度推进（行内公式）

**对应最底层代码：**

- GitHub链接（多`dt`模型配置与迁移初始化）：
  - /blob/main/Train.py#L156-L167
- GitHub链接（按不同`dt`组合自回归滚动）：
  - /blob/main/ModelClass.py#L463-L475

```python
# Train.py (L156-L167)
dts = int_list(args.dts)

# to initialize lazy modules:
for i, dt in enumerate(dts):

    if i==0:
        transformer = Transformer2D(**transformer_kwargs)
        a = transformer(model.encoder(torch.randn(1,1,64,64).to(Device)).cpu())
        model.set_model(transformer, dt=dt)
    else:
        # transfer learning
        model.set_model(copy.deepcopy(model.models[str(dts[i-1])]), dt=dt)
```

```python
# ModelClass.py (L463-L475)
for dt in sorted(dts, reverse=True):
    new_preds_idxs = []
    for t in preds_idxs:
        latent = preds[t]
        for i in range(t+dt, min(t+prev_dt, Dt+1), dt):
            if preds[i] is not None:
                break
            latent = self.models[str(dt)](latent)
            preds[i] = latent
            new_preds_idxs.append(i)
    preds_idxs += new_preds_idxs
    prev_dt = dt
```

**对应公式：**

$$
\Delta t=\{1,2,4,8\}
$$

$$
D^{\Delta t}
$$

---

### 2.10 Eq. (8) nRMSE损失

**对应最底层代码：**

- GitHub链接：
  - /blob/main/Model.py#L518-L539

```python
# Model.py (L518-L539)
def L2normLoss(pred, true, dim=(-1,-2,-3), mean=True):
    pred = torch.as_tensor(pred, dtype=torch.float)
    true = torch.as_tensor(true, dtype=torch.float)
    error = pred - true
    error_norm = torch.norm(error, p=2, dim=dim)
    x_norm = torch.norm(true, p=2, dim=dim)
    batched_loss = error_norm / x_norm
    if mean:
        return batched_loss.mean()
    return batched_loss
```

**对应公式：**

$$
nRMSE(\bar{z}, z)=\frac{\| \bar{z}-z\| _{2}}{\| z\| _{2}} \tag{8}
$$

---

### 2.11 优化器与学习率调度（行内公式）

**对应最底层代码：**

- GitHub链接（Adam默认学习率即`0.001`）：
  - /blob/main/ModelClass.py#L131-L131
  - /blob/main/ModelClass.py#L268-L268
- GitHub链接（scheduler factor=0.2, patience=5）：
  - /blob/main/ModelClass.py#L137-L139
  - /blob/main/ModelClass.py#L274-L276

```python
# ModelClass.py (L131, L137-L139)
opt = optimizer(self.AE.parameters(), **opt_kwargs)
...
scheduler = optim.lr_scheduler.ReduceLROnPlateau(opt,
                                                 factor=0.2,
                                                 patience=5)
```

```python
# ModelClass.py (L268, L274-L276)
opt = optimizer(model.parameters(), **opt_kwargs)
...
scheduler = optim.lr_scheduler.ReduceLROnPlateau(opt,
                                                 factor=0.2,
                                                 patience=5)
```

**对应公式：**

$$
\text{lr}=0.001
$$

$$
\text{factor}=0.2
$$

$$
\text{patience}=5
$$

---

## 3. Experiments and discussion

### 3.1 Navier–Stokes实验方程组（论文给出的数据生成方程）

**对应最底层代码（本仓库中不含NS数值求解器；这里只加载预先生成的数据）：**

- GitHub链接：
  - /blob/main/Train.py#L72-L85
  - /blob/main/utils.py#L46-L51

```python
# Train.py (L72-L85)
benchmark = args.benchmark
if benchmark == 1:
    file_name = 'MST_NS_v1e-3_N1200_T50.npy'
elif benchmark == 2:
    file_name = 'MST_NS_v1e-4_N1200_T30.npy'
elif benchmark == 3:
    file_name = 'MST_NS_v1e-5_N1200_T20.npy'

elif benchmark == 7:
    file_name = 'KFvorticity_Re40_N200_T500.npy'

if benchmark>0:
    data_array = load_data(file_name)
```

```python
# utils.py (L46-L51)
def load_data(file_name, tensor=True):
    data_path = os.path.join(DATA_DIR, file_name)
    if tensor:
        return torch.as_tensor(np.load(data_path), dtype=torch.float)
    else:
        return np.load(data_path)
```

**对应公式：**

$$
\partial_{t} \omega(x,t) + u(x,t) \cdot \nabla \omega(x,t) = \nu \Delta \omega(x,t) + f(x),\ x\in (0,1)^{2},\ t\in (0,T]
$$

$$
\nabla \cdot u(x, t)=0, x \in(0,1)^{2}, t \in(0, T]
$$

$$
\omega (x,0)=\omega _{0}(x), x\in (0,1)^{2}
$$

---

### 3.2 计算效率段落中的分辨率公式

**对应最底层代码（下采样4次把`64x64`压缩到`4x4`）：**

- GitHub链接（编码器每层`AvgPool2d(2)`）：
  - /blob/main/Model.py#L401-L412
- GitHub链接（默认`down=4`）：
  - /blob/main/Train.py#L30-L31

```python
# Model.py (L401-L412)
for i in range(n):
    layers += [
        nn.Conv2d(in_channels = channels[i],
                  out_channels = channels[i+1],
                  kernel_size = 3,
                  stride = 1,
                  padding = 'same',
                  padding_mode = padding_mode
                  ),
        nn.AvgPool2d(2),
        act()
        ]
```

```python
# Train.py (L30-L31)
parser.add_argument('-down', '--down', type=int, default=4,
                    choices=[2,3,4,5], help='Number of Encoder/Decoder stages')
```

**对应公式：**

$$
4 \times 4
$$

$$
64 \times 64
$$

---

## 4. Conclusion and future work

本章未新增可独立映射到代码的显式公式。

---

## 附：论文中其余出现的数学符号/表达式与代码落点

### A.1 注意力函数和值函数类型签名

**对应最底层代码：**

- GitHub链接：
  - /blob/main/Model.py#L122-L141
  - /blob/main/Model.py#L321-L326

```python
# Model.py (L122-L141)
self.net = nn.Sequential(
    nn.LazyLinear(hidden_dim),
    nn.LeakyReLU(),
    nn.Linear(hidden_dim, num_heads)
    )
```

```python
# Model.py (L321-L326)
self.Q = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
self.K = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
self.V = nn.Conv2d(dim, dim, 1, bias=qkv_bias,
                   groups = num_heads if split else 1)
```

**对应公式：**

$$
\kappa: \mathbb{R}^{2(d_{p}+d_{f})} \to \mathbb{R}
$$

$$
V: \mathbb{R}^{d_{f}} \to \mathbb{R}^{d_{f}}
$$

$$
\Delta f_{i}
$$

---

### A.2 论文中“编码器/解码器/时间推进”符号再次出现

**对应最底层代码：**

- GitHub链接：
  - /blob/main/ModelClass.py#L46-L49
  - /blob/main/ModelClass.py#L57-L59

```python
# ModelClass.py (L46-L49)
self.encoder = encoder.to(device)
self.decoder = decoder.to(device)
self.AE = nn.Sequential(self.encoder, self.decoder).to(device)
self.AE_train_loss = []
```

```python
# ModelClass.py (L57-L59)
self.models[str(dt)] = model.to(device)
self.train_losses[dt] = []
self.val_losses[dt] = []
```

**对应公式：**

$$
P, Q, D^{\Delta t}
$$

---

## 结论性说明（仅针对“是否有直接代码”）

1. 论文中的方法学核心公式（Eq. (3)–Eq. (8) 及其行内符号）在本仓库均有直接可追踪的底层实现。  
2. 论文中的PDE物理方程（Eq. (1)与实验中NS方程）在本仓库中不以显式数值离散器实现，而是作为数据生成背景；仓库对应代码是“加载数据并学习时间推进”。  
3. 本文档已按论文章节顺序覆盖`论文.md`内所有出现的公式/数学表达，并为每条给出可访问链接与代码片段。
